# 日期管理功能说明

## 概述

新的日期管理功能可以记住上次下载的日期，并基于该日期智能计算下次运行需要下载的时间范围，避免重复下载和遗漏新内容。

## 主要特性

1. **智能日期计算**：基于上次下载日期自动计算需要下载的天数范围
2. **最小天数保证**：确保每次至少下载1天的内容
3. **最大天数限制**：不会超过配置文件中设置的最大天数限制
4. **首次运行支持**：首次运行时使用完整的天数限制
5. **持久化存储**：下载日期记录保存在配置文件中

## 配置说明

在 `config/config.yml` 中新增了以下配置项：

```yaml
filtering:
  days_limit: 60                       # 下载多少天内发布的小说
  last_download_date: null             # 上次下载日期，格式：YYYY-MM-DD，null表示首次运行
  min_days_limit: 1                    # 最小下载天数限制
```

## 工作原理

### 首次运行
- `last_download_date` 为 `null`
- 使用 `days_limit` 设置的完整天数范围
- 例如：`days_limit: 60` 则下载60天内的小说

### 后续运行
- 计算距离 `last_download_date` 的天数
- 实际下载天数 = max(距离天数, `min_days_limit`)
- 但不超过 `days_limit` 的限制
- 下载完成后自动更新 `last_download_date` 为当前日期

### 示例场景

1. **每天运行**：
   - 上次：2026-01-31，今天：2026-02-01
   - 距离1天，下载1天内的小说

2. **间隔3天运行**：
   - 上次：2026-01-29，今天：2026-02-01
   - 距离3天，下载3天内的小说

3. **长时间未运行**：
   - 上次：2025-01-01，今天：2026-02-01
   - 距离396天，但限制在 `days_limit: 60`，下载60天内的小说

## 命令行工具

提供了 `date_tool.py` 工具来管理日期设置：

### 查看状态
```bash
python date_tool.py status
```

### 设置上次下载日期
```bash
python date_tool.py set 2026-01-31
```

### 重置日期（恢复首次运行状态）
```bash
python date_tool.py reset
```

### 预览下次运行范围
```bash
python date_tool.py preview
```

## 使用建议

1. **定期运行**：建议每天或每几天运行一次，这样可以最小化下载量
2. **首次设置**：如果你之前已经下载过小说，可以使用 `date_tool.py set` 设置合适的起始日期
3. **重置功能**：如果想要重新下载大量历史内容，可以使用 `reset` 命令
4. **监控日志**：程序运行时会显示日期相关信息，帮助你了解下载范围

## 兼容性

- 现有的单本下载功能不受影响
- 批量下载功能会自动使用新的日期管理
- 配置文件向后兼容，旧配置会自动添加新字段

## 故障排除

如果遇到日期相关问题：

1. 检查配置文件中的日期格式是否正确（YYYY-MM-DD）
2. 使用 `date_tool.py status` 查看当前状态
3. 如有必要，使用 `date_tool.py reset` 重置到初始状态